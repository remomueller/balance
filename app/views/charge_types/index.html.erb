<% @title = 'Charge Types' %>
<div class="page-header">
  <h1>
    <%= @title %>
    <%= link_to "Create Charge Type", new_charge_type_path, class: 'btn btn-xs btn-primary' %>
  </h1>
</div>

<%= form_tag charge_types_path, method: :get, remote: true, class: 'form-search', data: { object: 'form-load' } do %>
  <%= text_field_tag 'search', params[:search], class: 'search-query' %>
  <%= submit_tag 'Search', class: 'btn btn-primary' %>
<% end %>

<div class="search" id="charge_types"><center><%= image_tag "contour/ajax-loader.gif" %></center></div>
